@model IEnumerable<FINMUE.Models.Casa>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NumeroDeCasa)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MetrosCuadrados)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.InmuebleId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroDeCasa)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MetrosCuadrados)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.InmuebleId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CasaId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CasaId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CasaId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

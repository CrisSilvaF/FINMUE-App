@model IEnumerable<FINMUE.Models.Piso>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NumeroDePiso)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MetrosCuadrados)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroDePiso)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MetrosCuadrados)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.PisoId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.PisoId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.PisoId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>

@model IEnumerable<FINMUE.Models.Local>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NumeroDeLocal)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.MetrosCuadrados)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tipo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NumeroDeLocal)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.MetrosCuadrados)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tipo)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.LocalId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.LocalId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.LocalId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
